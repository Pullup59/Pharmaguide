import { ProductPostRequest } from './product-post-request.model';

describe('ProductPostRequest', () => {
  it('should create an instance', () => {
    expect(new ProductPostRequest()).toBeTruthy();
  });
});
